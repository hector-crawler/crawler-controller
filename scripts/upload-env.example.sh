#!/bin/bash

SSH_USERNAME="< the username to use for login >"
SSH_HOSTADDRESS="< address to the RPi (usually the local IP address) >"
SAVE_PATH="< the path to save the repo to (usually /home/<USERNAME>/crawler) >"
